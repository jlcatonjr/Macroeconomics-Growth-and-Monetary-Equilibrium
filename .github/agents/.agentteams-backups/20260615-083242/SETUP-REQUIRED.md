<!-- AGENTTEAMS:BEGIN content v=1 -->
# SETUP-REQUIRED.md

The following **67 placeholder(s)** could not be automatically resolved
for project **MacroeconomicsGrowthMonetaryEquilibrium** and require manual attention.

---

## 1. `{CONVERSION_PIPELINE}`

**Found in:** `multiple`
**Context:** The placeholder {CONVERSION_PIPELINE} could not be auto-resolved.

**Action required:** Search for `{MANUAL:CONVERSION_PIPELINE}` across all generated
agent files and replace with the correct value.

---

## 2. `{PIP_PACKAGE_NAME}`

**Found in:** `multiple`
**Context:** The placeholder {PIP_PACKAGE_NAME} could not be auto-resolved.

**Action required:** Search for `{MANUAL:PIP_PACKAGE_NAME}` across all generated
agent files and replace with the correct value.

---

## 3. `{DOC_SITE_CONFIG_FILE}`

**Found in:** `multiple`
**Context:** The placeholder {DOC_SITE_CONFIG_FILE} could not be auto-resolved.

**Action required:** Search for `{MANUAL:DOC_SITE_CONFIG_FILE}` across all generated
agent files and replace with the correct value.

---

## 4. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ipywidgets-reference.md`
**Context:** The reference file for 'ipywidgets' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 5. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ipywidgets-reference.md`
**Context:** The reference file for 'ipywidgets' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 6. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ipywidgets-reference.md`
**Context:** The reference file for 'ipywidgets' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 7. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-networkx-reference.md`
**Context:** The reference file for 'networkx' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 8. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-networkx-reference.md`
**Context:** The reference file for 'networkx' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 9. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-networkx-reference.md`
**Context:** The reference file for 'networkx' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 10. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-scipy-reference.md`
**Context:** The reference file for 'scipy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 11. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-scipy-reference.md`
**Context:** The reference file for 'scipy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 12. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-scipy-reference.md`
**Context:** The reference file for 'scipy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 13. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-statsmodels-reference.md`
**Context:** The reference file for 'statsmodels' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 14. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-statsmodels-reference.md`
**Context:** The reference file for 'statsmodels' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 15. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-statsmodels-reference.md`
**Context:** The reference file for 'statsmodels' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 16. `{COMPONENT_SPEC}`

**Found in:** `ch1-purposive-action-expert.agent.md`
**Context:** Component 'Chapter 1 - Purposive Action' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 17. `{COMPONENT_SECTIONS}`

**Found in:** `ch1-purposive-action-expert.agent.md`
**Context:** Component 'Chapter 1 - Purposive Action' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 18. `{COMPONENT_SOURCES}`

**Found in:** `ch1-purposive-action-expert.agent.md`
**Context:** Component 'Chapter 1 - Purposive Action' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 19. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch1-purposive-action-expert.agent.md`
**Context:** Component 'Chapter 1 - Purposive Action' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 20. `{COMPONENT_SPEC}`

**Found in:** `ch2-entrepreneurship-supply-demand-expert.agent.md`
**Context:** Component 'Chapter 2 - Entrepreneurship and Supply and Demand' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 21. `{COMPONENT_SECTIONS}`

**Found in:** `ch2-entrepreneurship-supply-demand-expert.agent.md`
**Context:** Component 'Chapter 2 - Entrepreneurship and Supply and Demand' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 22. `{COMPONENT_SOURCES}`

**Found in:** `ch2-entrepreneurship-supply-demand-expert.agent.md`
**Context:** Component 'Chapter 2 - Entrepreneurship and Supply and Demand' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 23. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch2-entrepreneurship-supply-demand-expert.agent.md`
**Context:** Component 'Chapter 2 - Entrepreneurship and Supply and Demand' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 24. `{COMPONENT_SPEC}`

**Found in:** `ch3-elements-macroeconomics-expert.agent.md`
**Context:** Component 'Chapter 3 - The Elements of Macroeconomics' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 25. `{COMPONENT_SECTIONS}`

**Found in:** `ch3-elements-macroeconomics-expert.agent.md`
**Context:** Component 'Chapter 3 - The Elements of Macroeconomics' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 26. `{COMPONENT_SOURCES}`

**Found in:** `ch3-elements-macroeconomics-expert.agent.md`
**Context:** Component 'Chapter 3 - The Elements of Macroeconomics' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 27. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch3-elements-macroeconomics-expert.agent.md`
**Context:** Component 'Chapter 3 - The Elements of Macroeconomics' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 28. `{COMPONENT_SPEC}`

**Found in:** `ch4-aggregate-supply-growth-expert.agent.md`
**Context:** Component 'Chapter 4 - Aggregate Supply, Technology, and Economic Growth' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 29. `{COMPONENT_SECTIONS}`

**Found in:** `ch4-aggregate-supply-growth-expert.agent.md`
**Context:** Component 'Chapter 4 - Aggregate Supply, Technology, and Economic Growth' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 30. `{COMPONENT_SOURCES}`

**Found in:** `ch4-aggregate-supply-growth-expert.agent.md`
**Context:** Component 'Chapter 4 - Aggregate Supply, Technology, and Economic Growth' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 31. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch4-aggregate-supply-growth-expert.agent.md`
**Context:** Component 'Chapter 4 - Aggregate Supply, Technology, and Economic Growth' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 32. `{COMPONENT_SPEC}`

**Found in:** `ch5-money-expert.agent.md`
**Context:** Component 'Chapter 5 - Money' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 33. `{COMPONENT_SECTIONS}`

**Found in:** `ch5-money-expert.agent.md`
**Context:** Component 'Chapter 5 - Money' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 34. `{COMPONENT_SOURCES}`

**Found in:** `ch5-money-expert.agent.md`
**Context:** Component 'Chapter 5 - Money' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 35. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch5-money-expert.agent.md`
**Context:** Component 'Chapter 5 - Money' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 36. `{COMPONENT_SPEC}`

**Found in:** `ch6-aggregate-demand-expert.agent.md`
**Context:** Component 'Chapter 6 - Aggregate Demand' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 37. `{COMPONENT_SECTIONS}`

**Found in:** `ch6-aggregate-demand-expert.agent.md`
**Context:** Component 'Chapter 6 - Aggregate Demand' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 38. `{COMPONENT_SOURCES}`

**Found in:** `ch6-aggregate-demand-expert.agent.md`
**Context:** Component 'Chapter 6 - Aggregate Demand' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 39. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch6-aggregate-demand-expert.agent.md`
**Context:** Component 'Chapter 6 - Aggregate Demand' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 40. `{COMPONENT_SPEC}`

**Found in:** `ch7-monetary-dynamics-expert.agent.md`
**Context:** Component 'Chapter 7 - Monetary Dynamics and Aggregate Analysis' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 41. `{COMPONENT_SECTIONS}`

**Found in:** `ch7-monetary-dynamics-expert.agent.md`
**Context:** Component 'Chapter 7 - Monetary Dynamics and Aggregate Analysis' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 42. `{COMPONENT_SOURCES}`

**Found in:** `ch7-monetary-dynamics-expert.agent.md`
**Context:** Component 'Chapter 7 - Monetary Dynamics and Aggregate Analysis' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 43. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch7-monetary-dynamics-expert.agent.md`
**Context:** Component 'Chapter 7 - Monetary Dynamics and Aggregate Analysis' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 44. `{COMPONENT_SPEC}`

**Found in:** `ch8-money-credit-loanable-funds-expert.agent.md`
**Context:** Component 'Chapter 8 - Money and Credit - The Loanable Funds Market' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 45. `{COMPONENT_SECTIONS}`

**Found in:** `ch8-money-credit-loanable-funds-expert.agent.md`
**Context:** Component 'Chapter 8 - Money and Credit - The Loanable Funds Market' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 46. `{COMPONENT_SOURCES}`

**Found in:** `ch8-money-credit-loanable-funds-expert.agent.md`
**Context:** Component 'Chapter 8 - Money and Credit - The Loanable Funds Market' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 47. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch8-money-credit-loanable-funds-expert.agent.md`
**Context:** Component 'Chapter 8 - Money and Credit - The Loanable Funds Market' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 48. `{COMPONENT_SPEC}`

**Found in:** `ch9-is-lm-expert.agent.md`
**Context:** Component 'Chapter 9 - IS-LM' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 49. `{COMPONENT_SECTIONS}`

**Found in:** `ch9-is-lm-expert.agent.md`
**Context:** Component 'Chapter 9 - IS-LM' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 50. `{COMPONENT_SOURCES}`

**Found in:** `ch9-is-lm-expert.agent.md`
**Context:** Component 'Chapter 9 - IS-LM' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 51. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch9-is-lm-expert.agent.md`
**Context:** Component 'Chapter 9 - IS-LM' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 52. `{COMPONENT_SPEC}`

**Found in:** `ch10-labor-expert.agent.md`
**Context:** Component 'Chapter 10 - Labor' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 53. `{COMPONENT_SECTIONS}`

**Found in:** `ch10-labor-expert.agent.md`
**Context:** Component 'Chapter 10 - Labor' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 54. `{COMPONENT_SOURCES}`

**Found in:** `ch10-labor-expert.agent.md`
**Context:** Component 'Chapter 10 - Labor' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 55. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch10-labor-expert.agent.md`
**Context:** Component 'Chapter 10 - Labor' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 56. `{COMPONENT_SPEC}`

**Found in:** `ch11-central-banking-expert.agent.md`
**Context:** Component 'Chapter 11 - Central Banking' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 57. `{COMPONENT_SECTIONS}`

**Found in:** `ch11-central-banking-expert.agent.md`
**Context:** Component 'Chapter 11 - Central Banking' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 58. `{COMPONENT_SOURCES}`

**Found in:** `ch11-central-banking-expert.agent.md`
**Context:** Component 'Chapter 11 - Central Banking' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 59. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `ch11-central-banking-expert.agent.md`
**Context:** Component 'Chapter 11 - Central Banking' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 60. `{COMPONENT_SPEC}`

**Found in:** `advanced-macroeconomics-expert.agent.md`
**Context:** Component 'Advanced Macroeconomics Course Notes' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 61. `{COMPONENT_SECTIONS}`

**Found in:** `advanced-macroeconomics-expert.agent.md`
**Context:** Component 'Advanced Macroeconomics Course Notes' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 62. `{COMPONENT_SOURCES}`

**Found in:** `advanced-macroeconomics-expert.agent.md`
**Context:** Component 'Advanced Macroeconomics Course Notes' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 63. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `advanced-macroeconomics-expert.agent.md`
**Context:** Component 'Advanced Macroeconomics Course Notes' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 64. `{COMPONENT_SPEC}`

**Found in:** `data-homework-expert.agent.md`
**Context:** Component 'Data Homework Assignments' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 65. `{COMPONENT_SECTIONS}`

**Found in:** `data-homework-expert.agent.md`
**Context:** Component 'Data Homework Assignments' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 66. `{COMPONENT_SOURCES}`

**Found in:** `data-homework-expert.agent.md`
**Context:** Component 'Data Homework Assignments' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 67. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `data-homework-expert.agent.md`
**Context:** Component 'Data Homework Assignments' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---


Once all items above are resolved, invoke `@conflict-auditor` to verify consistency.
<!-- AGENTTEAMS:END content -->
